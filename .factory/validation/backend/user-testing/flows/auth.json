{
  "groupId": "auth",
  "testedAt": "2026-03-25T00:00:00Z",
  "isolation": {
    "apiBaseUrl": "http://localhost:8020",
    "authCredentials": "admin:admin (HTTP Basic Auth)",
    "testingTool": "curl"
  },
  "toolsUsed": ["curl"],
  "assertions": [
    {
      "id": "VAL-AUTH-001",
      "title": "Login with valid credentials",
      "status": "pass",
      "steps": [
        { "action": "POST /api/v1/token/login with HTTP Basic Auth admin:admin", "expected": "HTTP 200 with access_token and refresh_token", "observed": "HTTP 200 returned JSON with both access_token and refresh_token JWT strings" }
      ],
      "evidence": {
        "screenshots": [],
        "consoleErrors": "N/A (curl test)",
        "network": "POST /api/v1/token/login -> 200, response: {\"access_token\":\"<JWT>\",\"refresh_token\":\"<JWT>\"}",
        "files": ["backend/auth/VAL-AUTH-001-login.txt"]
      },
      "issues": null
    },
    {
      "id": "VAL-AUTH-002",
      "title": "Token refresh",
      "status": "pass",
      "steps": [
        { "action": "POST /api/v1/token/login to get refresh_token", "expected": "Obtain refresh_token", "observed": "Got refresh_token JWT string" },
        { "action": "POST /api/v1/token/refresh with Bearer refresh_token", "expected": "HTTP 200 with new access_token", "observed": "HTTP 200 returned JSON with new access_token" }
      ],
      "evidence": {
        "screenshots": [],
        "consoleErrors": "N/A (curl test)",
        "network": "POST /api/v1/token/refresh -> 200, response: {\"access_token\":\"<NEW_JWT>\"}",
        "files": ["backend/auth/VAL-AUTH-002-refresh.txt"]
      },
      "issues": null
    },
    {
      "id": "VAL-AUTH-003",
      "title": "Unauthorized request rejected",
      "status": "pass",
      "steps": [
        { "action": "GET /api/v1/profit without Authorization header", "expected": "HTTP 401 Unauthorized", "observed": "HTTP 401 with {\"detail\":\"Not authenticated\"}" }
      ],
      "evidence": {
        "screenshots": [],
        "consoleErrors": "N/A (curl test)",
        "network": "GET /api/v1/profit -> 401",
        "files": ["backend/auth/VAL-AUTH-003-unauthorized.txt"]
      },
      "issues": null
    },
    {
      "id": "VAL-AUTH-004",
      "title": "Ping endpoint (no auth required)",
      "status": "pass",
      "steps": [
        { "action": "GET /api/v1/ping without any auth", "expected": "HTTP 200 with {\"status\":\"pong\"}", "observed": "HTTP 200 with {\"status\":\"pong\"}" }
      ],
      "evidence": {
        "screenshots": [],
        "consoleErrors": "N/A (curl test)",
        "network": "GET /api/v1/ping -> 200, response: {\"status\":\"pong\"}",
        "files": ["backend/auth/VAL-AUTH-004-ping.txt"]
      },
      "issues": null
    },
    {
      "id": "VAL-AUTH-005",
      "title": "Show config returns webserver mode",
      "status": "pass",
      "steps": [
        { "action": "GET /api/v1/show_config with Bearer access_token", "expected": "HTTP 200 with runmode=webserver, stake_currency=CNY, strategy name, api_version>=2.34, exchange, bot_name, timeframe", "observed": "HTTP 200 with all required fields: runmode=webserver, stake_currency=CNY, strategy=chan_theory, api_version=2.34, exchange=ashare, bot_name=A-Share Quant Dashboard, timeframe=1d" }
      ],
      "evidence": {
        "screenshots": [],
        "consoleErrors": "N/A (curl test)",
        "network": "GET /api/v1/show_config -> 200, response has all 7 required fields",
        "files": ["backend/auth/VAL-AUTH-005-show-config.txt"]
      },
      "issues": null
    },
    {
      "id": "VAL-AUTH-006",
      "title": "Version endpoint",
      "status": "pass",
      "steps": [
        { "action": "GET /api/v1/version", "expected": "HTTP 200 with version string", "observed": "HTTP 200 with {\"version\":\"1.0.0\"}" }
      ],
      "evidence": {
        "screenshots": [],
        "consoleErrors": "N/A (curl test)",
        "network": "GET /api/v1/version -> 200, response: {\"version\":\"1.0.0\"}",
        "files": ["backend/auth/VAL-AUTH-006-version.txt"]
      },
      "issues": null
    }
  ],
  "frictions": [],
  "blockers": [],
  "summary": "Tested 6 assertions: 6 passed, 0 failed. All auth and system endpoints behave as specified."
}
